# Piper voice models

Place Piper **`.onnx`** and matching **`.onnx.json`** files here (same basename), or run from `iqbal_ai_stg`:

```bash
bash scripts/download_piper_models.sh
```

See **`docs/piper_models_setup.md`** for URLs, environment variables, and production notes.
